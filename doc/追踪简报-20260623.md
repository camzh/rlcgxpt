# 📊 润六尺供需平台每日巡检报告 2026-06-23

**报告时间：** 2026-06-23 17:44  
**巡检客户端：** 本地开发机  
**覆盖范围：** E:\rlcgxpt（小程序）、E:\网页端（网页端）、rlcgxpt.com（线上）

---

## 一、网站可达性 ✅

| 检测项 | 状态 | 响应时间 |
|--------|------|---------|
| https://rlcgxpt.com | ✅ 正常 | 72ms |
| /api/me 接口 | ✅ 正常响应 | < 50ms |

线上服务正常，页面可访问，API端点响应迅速。

---

## 二、前端丝滑度分析

### ✅ 当前做的好的

- **server.js 引入环境变量**：JUZHEN_ADMIN_PASSWORD / JUZHEN_VIEW_PASSWORD / JUZHEN_MINI_PROGRAM_TOKEN 均从环境变量注入，不使用硬编码
- **密码策略**：密码至少10位，必须包含字母和数字，不能包含手机号后6位或姓名
- **会话过期机制**：SESSION_MAX_AGE_MS = 24h，登录失败5次锁定15分钟
- **小程序端云同步**：app.js 实现了2分钟定时同步 + onShow/onHide 前后台同步联动
- **API 请求超时**：小程序请求 5s 超时，有专门的超时错误提示

### ⚠️ 需要关注的

1. **cloud-api.js 硬编码 TOKEN 问题（高危）🔴**
   - `const LEGACY_COMPAT_TOKEN = "<redacted>"` — 明文硬编码在源码中
   - 作为后端 API 认证的后备鉴权凭证，暴露风险极高
   - 虽然 server.js 已改为环境变量，但小程序端 cloud-api.js 仍保留硬编码
   - **建议**：将其迁移到小程序配置或环境变量中，或完全废弃该旧兼容TOKEN

2. **初始密码安全**
   - `INITIAL_USER_PASSWORD = '123456'` — 59人种子数据的初始密码
   - 虽然要求首次登录后强制修改密码（mustChangePassword），但明文初始密码在源码中可见
   - **建议**：随机生成初始密码或移到环境变量

3. **无 HTTPS 降级保护**
   - HTML 页面没有设置 Strict-Transport-Security 头
   - 无 CSP（Content-Security-Policy）头
   - **建议**：添加 HSTS 和 CSP 头部

---

## 三、信息安全风险

### 🔴 高风险

| 风险项 | 严重程度 | 说明 |
|--------|---------|------|
| LEGACY_COMPAT_TOKEN 硬编码 | 🔴 高危 | 53位随机字符串明文暴露在 cloud-api.js |
| 初始密码123456源码可见 | 🟡 中危 | 虽然强制改密，但密码在git历史中永久留存 |
| 无数据库，仅JSON文件存储 | 🟡 中危 | db.json 无并发写保护，文件损坏风险 |
| 用户手机号明文存储 | 🟡 中危 | 59人手机号在 server.js 中明文硬编码 |

### 🟢 已做的防护

- ✅ 密码使用 salt + hash 存储（`hashPassword()`）
- ✅ 登录失败锁定机制（5次失败，15分钟锁定）
- ✅ 会话使用 HttpOnly + Secure cookie
- ✅ 审批制注册（超管/组长审批）
- ✅ 敏感操作需重新验证密码
- ✅ 小程序端使用 Bearer Token 认证

---

## 四、数据安全

| 维度 | 状态 | 评价 |
|------|------|------|
| 数据持久化 | JSON 文件 → db.json | ⚠️ 生产环境应该用数据库 |
| 数据备份 | 无自动备份机制 | ⚠️ db.json 崩溃即丢失 |
| 用户数据保护 | 无数据脱敏接口 | ⚠️ 公共 API 返回手机号 |

---

## 五、代码与部署状态

### Git 变更情况

**小程序端（E:\rlcgxpt）** — 自 6/21 以来有新开发：

```
M  app.js (140行)    ← 核心启动逻辑更新
M  services/detail/  (520行大改)
M  services/logs/    (368行新增)
M  services/auth.js  (195行)
M  services/cloud-api.js (449行)
M  services/demand.js   (525行)
M  services/inventory.js (782行) ← 最大变动
?? cloudfunctions/    ← 新目录（腾讯云函数SDK）
?? pages/privacy/     ← 新页面
?? pages/terms/       ← 新页面
```

共 **33 个文件修改**（+4007 / -1006），**8 个未追踪新文件**。

**网页端（E:\网页端）** — 6/23 当天有更新：
- `server.js` 最后修改：2026-06-23 10:53
- `index.html` 最后修改：2026-06-23 14:23
- 无 Git 仓库追踪

---

## 六、各维度综合评分

| 维度 | 评分 | 说明 |
|------|------|------|
| 前端丝滑度 | 🟢 **良好** | API响应72ms，有云同步机制，5s超时保护 |
| 信息安全 | 🟡 **需改进** | LEGACY_TOKEN 硬编码是严重隐患 |
| 网络安全 | 🟢 **基础防护** | HTTPS运行、Bearer认证、密码策略，缺HSTS/CSP |
| 数据安全 | 🟡 **需关注** | 无数据库、无备份、手机号明文存储 |
| 代码质量 | 🟡 **活跃开发** | 33文件未提交，建议定期commit |
| 部署稳定性 | 🟢 **正常运行** | 线上服务可达，API响应正常 |

---

## 七、重点风险 & 行动建议

### 🔴 立即处理

1. **移除 LEGACY_COMPAT_TOKEN 硬编码**（30分钟工作量）
   - 从 cloud-api.js 移除明文 TOKEN
   - 小程序通过云函数环境变量注入或废弃该兼容Token

2. **为小程序端建立 Git 仓库定期提交策略**
   - 目前33个文件1000+行改动未提交
   - 建议每天/每次功能完成时提交一次

### 🟡 本周处理

3. **数据库化** — db.json 适合原型但已到55+KB，用户数据59+人
4. **db.json 自动备份** — 至少每5分钟增量备份到 backup/ 目录
5. **给 index.html 添加 HSTS 和 CSP 头**

### 🟢 持续关注

6. 小程序云同步机制的稳定性（2分钟定时器）
7. 网页端 index.html 的前端体验优化（当前是71KB的单页）
8. 报警机制：网站宕机时能自动通知

---

## 八、与上次报告对比

| 对比项 | 6/21 报告 | 6/23 现状 | 变化 |
|--------|----------|----------|------|
| 小程序变更 | 30文件，+3212行 | 33文件，+4007行 | 继续演进 |
| 网站运行 | 线上可达 | ✅ 正常 (72ms) | 稳定 |
| 安全体系 | 环境变量化 | 仍存在硬编码 TOKEN | 未完全解决 |
| 数据持久化 | db.json | 同，无变化 | 无进展 |

---

*本报告由自动巡检系统生成，北京时间 2026-06-23 17:47*
